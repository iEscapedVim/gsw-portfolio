---
import { getCollection } from 'astro:content';
import StatsCard from "@comps/statsCard.astro";
const allStats = await getCollection("stats");
---
<div class="grid grid-cols-2 md:grid-cols-4 gap-10 bg-gray-800 py-8 my-12">
    {allStats.map((p) => <StatsCard post={p}/>)}
</div>